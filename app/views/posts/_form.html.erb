<%= form_for @post do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %>
  <%= a.label :body %>
  <%= a.text_area :body %>
  <%= a.label :author %>
  <%= a.text_field :author %>
  <%= a.submit %>
<%end%>
