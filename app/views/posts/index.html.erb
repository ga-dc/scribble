
<% @posts.each do |post| %>
<p class="ptitle"><%= post.title%></p>
<p class="pauthor"><%= post.author%></p>
<div class="go_postal">
  <p class="pcontent"><%= post.post_content%></p>
  <p><%= link_to"see more",post_path(post),:class => "a"%></p>
</div>
<% end %>
