<h1 id="blogr">Blog<span class="glyphicon glyphicon-registration-mark  " aria-hidden="true"></span></h1>
<%= link_to 'New post', new_post_path, class: "btn btn-primary btn-lg sexy-new-post" %>
<div class="panel panel-info" id="sexy-panel">
    <div class="panel-heading">
      <h5><mark>Create sexy posts!</mark></h4>
    </div>
          <% @posts.each do |post| %>
          <div class="well index-post">
              <h2><%= post.title %></h2>
              <p><%= post.text %></p>
              <span><%= link_to 'Show', post_path(post), class: 'btn btn-default' %></span>
              <span><%= link_to 'Edit', edit_post_path(post), class: 'btn btn-info' %></span>
              <span><%= link_to 'Destroy', post_path(post), class: 'btn btn-danger', method: :delete,
                                data: { confirm: 'Are you sure?' } %></td>
            </span>
          </div>
          <% end %>
    </div>
