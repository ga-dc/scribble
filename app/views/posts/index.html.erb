<h3> All of your Posts </h3>
<% @posts.each do |post| %>
  <p>
    <%= link_to post.title, post_path(post)%>,
    <%=  post.title %> | <i><%= link_to "Delete this post", post_path(post ), method: :delete %><i>
  </p>
  <p>
    <%=  post.body %>
  </p>
  <%= "_" * 40 %>
<% end %>
