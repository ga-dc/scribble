<h1>All Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to "#{post.title}", post_path(post.id) %>
    </li>
  <% end %>
</ul>

<p> <%= link_to "New Post", new_post_path %> </p>
