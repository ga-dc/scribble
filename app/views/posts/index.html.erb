<h1>All Posts</h1>
<%=link_to "New Post", new_post_path%>
<hr>
<section>
<%@posts.reverse_each do |post|%>
  <section>
    <h2><%=post.title%></h2>
    <h3>By <%=post.author%></h3>
    <p>Posted at <%=post.created_at%></p>
    <p><%=post.text[0...200]%><%if post.text.length>200%>...<%end%></p>
    <%if post.updated_at != post.created_at%><p>Updated at <%=post.updated_at%></p><%end%>
  </section>
  <%=link_to "Read More/Add Comment(#{post.comments.length})", post_path(post)%>
  <hr>
<%end%>
</section>
