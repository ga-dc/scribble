<h1>Entries</h1>
<div class="container">
<% @posts.each do |post| %>
<div class="post">
<h2><%= post.title.capitalize %></h2>
<section><%= post.text[0..500] %>
  <span class="readmore"><%= link_to "...read more", post_path(post) %></span>
</div>
<% end %>
</div>
