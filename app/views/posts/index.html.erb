
<% @posts.each do |post| %>
    <h2><%= link_to post.title, post_path(post) %></h2>

    <p><%= post.content %></p>
    <p><time>Posted at <%= post.created_at %></time></p>
    <p><time>Modified at <%= post.updated_at %></time></p>
    <p>By <%= post.author %></p>
    <hr>
<%end%>
