<h2><%= @post.title %> </h2>
<h3>By <%= @post.author %> </h3>

  <ul>
    <% @post.comments.each do |comment| %>
      <li>
        <%= comment.subject %>
      </li>
    <% end %>
  </ul>

  <%= link_to "Create New Post", new_post_path %>
