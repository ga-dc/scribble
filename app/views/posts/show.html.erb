<h2><%= @post.title %></h2>
<h2> <%= link_to "edit", edit_post_path(@post) %></h2>
<h4>By: <%=@post.author %></h4>
<p><%=@post.post%></p>
<p><%= link_to "Add Comment", new_comment_path(@comment) %></p>
<ul>
  <% @post.comments.each do |comment| %>
  <li><%= link_to comment.id, comment_path(comment) %>
  hello </li>
  <% end %>
</ul>
