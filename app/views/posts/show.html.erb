<h2><%= @post.title %>
    <a href="/posts/<%= @post.id %>/edit">(edit)</a>
</h2>
<h4><%= @post.title %></h4>

<h3>Comments</h3>
<ul>
    <% @post.comments.each do |comment| %>
    <li>
        <%= link_to "View", post_comment_path(@post, comment) %>
        <%= comment.content %>
    </a>
</li>

<% end %>
</ul>
