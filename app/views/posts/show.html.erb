<h1><%= @post.title %></h1>
<p><%= @post.body %></p>

<ul>
<% @post.comments.each do |comment| %>
  <li>
    <%= comment.comment_text %>
  </li>
<% end %>
</ul>

<!-- <%= form_for @post, method: :delete do |f| %>
  <%= f.submit "Delete Post", action: 'destory', class: "delete" %>
<% end %> -->
<%= button_to "Delete Post", { action: "destroy", id: @post.id }, method: :delete %>
<%= button_to "Edit Post",  { action: "edit", id: @post.id }, method: :get %>
<%= button_to "New Comment", { action: "new", controller: "comments", id: @post.id }%>
