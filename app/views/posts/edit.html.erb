<h1>Edit Post</h1>

<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.submit "Edit" %>
<% end %>

<%= form_for @post, method: :delete do |f| %>
	<%= f.submit "Delete", class: "delete" %>
<% end %>
