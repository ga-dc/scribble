<h2>comments <%= link_to "(+)", new_post_comment_path(@post) %></h2>

<ul>
  <% @comment.each do |comment| %>
    <li>
      <%= link_to "#{comment.cContent}", post_comment_path(@post, comment) %>
    </li>
  <% end %>
</ul>