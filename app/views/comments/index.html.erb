<h2>Comments <%= link_to "Add Comment", new_post_comment_path %></h2>

<ul>
  <% @comments.each do |comment| %>
  <li>
    <%= link_to "Comments", post_comments_path(@post, @comment) %>
  </li>
  <% end %>
</ul>
