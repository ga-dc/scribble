<h1>Comments <a href="/comments/new">(+)</a></h1>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <a href="/comments/<%= comment.id %>">
        <%= comment.author %> <%= comment.post.name %>
      </a>
    </li>
  <% end %>
</ul>
