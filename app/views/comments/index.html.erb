<h2>Comments <%= link_to "(+)", new_post_comment_path(@post) %></h2>


<%= @comments.each do |comment| %>
  <div>
    <p><%= comment.content %></p>
    <p><%= comment.author %></p>
  </div>
<% end %>
