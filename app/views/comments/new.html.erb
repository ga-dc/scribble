<h3> Add a new Comment! </h3>
  <%=form_for ([@post, @comment]) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %> <br>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Submit" %>

  <% end %>
