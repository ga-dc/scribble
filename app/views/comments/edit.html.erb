<%= form_for([@posts, @posts.comments.build]) do |f| %>
    <%= f.label :author %>
    <%= f.text_field :author %>

    <%= f.label :content %>
    <%= f.text_field :content %>

   <%= f.submit %>
   <% end %>
