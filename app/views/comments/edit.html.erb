<h1>Edit Comment</h1>

<%= form_for [@post, @comment] do |f| %>

  <%= f.label :comment_text %>
  <%= f.text_field :comment_text  %>

  <%= f.submit %>

<% end %>

<%= link_to 'Delete!', post_comment_path(@post, @comment), method: :delete, class: 'delete' %>
