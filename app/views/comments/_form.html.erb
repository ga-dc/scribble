<%= form_for([@post, @comment]) do |f| %>
  <img class="ident" src="<%= current_user.identicon %>" alt="Identicon">
  <p class="subTitle">
    by <%= @user.username %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
