<%= form_for([@post, @comment]) do |f| %>
  <p class="subTitle">
    by <%= @user.username %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
