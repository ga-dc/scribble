<%= form_for([@post, @comment]) do |f| %>

  <%= f.label :user %>
  <%= f.text_field :user %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Chime In!" %>
<% end %>
