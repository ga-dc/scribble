<%= @post.title %>
<%= @post.created_at %>
<%= @post.content %>
<%= link_to "Edit/Delete", edit_post_path %>

<% @post.comments.each do |comment| %>
  <div>
    <%= comment.title %>
    <%= comment.content %>
  </div>
<% end %>
